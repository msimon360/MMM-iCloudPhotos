# Copy to .env and fill in. Never commit .env or put the password in config.js.
ICLOUD_USER=you@example.com
# Wrap in single quotes if the password has $, !, #, spaces, or other special characters.
# Do not backslash-escape those characters; Python reads this file, the shell does not.
ICLOUD_PASSWORD='your-apple-id-password'
